{
  "description": "Opt-in Codex completion and attention notifications for this workspace.",
  "hooks": {
    "PermissionRequest": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "cd \"$(git rev-parse --show-toplevel)\" && /usr/bin/python3 -m codex_discord.attention_hook",
            "timeout": 7,
            "statusMessage": "Sending attention request to Discord"
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "cd \"$(git rev-parse --show-toplevel)\" && /usr/bin/python3 -m codex_discord.stop_hook",
            "timeout": 7,
            "statusMessage": "Sending completion to Discord"
          }
        ]
      }
    ]
  }
}
